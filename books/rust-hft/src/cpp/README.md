# 现代 C++：从零基础到低延迟系统

C++ 是一门既能写普通业务程序，也能直接管理内存、线程和机器资源的语言。很多交易所接入、行情处理、撮合和风控系统仍然使用 C++，不是因为“老代码只能如此”，而是因为它允许工程师非常明确地控制对象放在哪里、何时创建和销毁，以及热路径上要不要发生分配。

这部分以 **C++20** 为基线，从读懂第一段最小语法和亲手编译程序讲起。你不需要提前懂 C，也不需要先记住一大堆语法。每章都先建立直觉，再给术语和可运行代码，最后把它放回低延迟工程里讨论。

> 本部分目标：能读懂现代 C++ 项目中的基础代码，能解释对象的所有权和生命周期，能判断复制、分配、容器扩容等成本，并为后续的模板、并发、无锁结构和性能优化打好基础。

## 先看全局：一段 C++ 程序怎样跑起来

```mermaid
flowchart LR
    A[源代码 .cpp] --> B[编译器检查并翻译]
    B --> C[目标文件 .o]
    C --> D[链接器组合库和目标文件]
    D --> E[可执行文件]
    E --> F[操作系统启动进程]
```

初学时最容易把三个问题混在一起：

- **语法是否正确**：由编译器先检查；
- **函数是否真的有实现**：通常在链接时确认；
- **程序运行是否符合预期**：只有运行、测试和观测才能回答。

“编译成功”只说明程序满足了一部分静态规则，并不说明它没有越界、悬空引用、数据竞争或业务错误。现代 C++ 提供了 RAII、标准容器和智能指针来减少这类风险，但仍需要程序员主动建立清楚的不变量。

## 学习路线

建议按下面的顺序阅读。后面的章节会使用前面已经解释过的词，但不会默认你了解本书其他部分的项目细节。

1. **[最小语法与数据建模](minimal_syntax.md)**：先学会读变量、函数、判断、循环、`struct` 和 `class`，不要求任何 C 语言基础。
2. **[编译、链接与内存直觉](compilation_memory.md)**：知道代码如何变成程序，区分自动存储期、动态存储期和静态存储期。
3. **[指针、引用与 `const`](pointers_references.md)**：知道“对象”和“对象的地址”不是一回事，能表达可空与不可空的借用。
4. **[RAII 与生命周期](raii_lifetime.md)**：让资源跟着对象自动释放，理解构造和析构。
5. **[复制与移动](copy_move.md)**：看懂对象为什么有时被复制、有时转移资源，以及 `std::move` 到底做了什么。
6. **[智能指针](smart_pointers.md)**：用类型表达独占、共享和弱观察关系。
7. **[STL 成本模型](stl_cost_model.md)**：会使用 `vector`、`array`、`string`、映射和算法，同时知道分配、扩容与缓存局部性。

接下来再读[模板与多态](templates_polymorphism.md)、[内存布局](memory_layout.md)、[错误与未定义行为](errors_ub.md)、[原子内存模型](atomics_memory_model.md)、[无锁结构](lock_free.md)、[分配器与零拷贝](allocators_zero_copy.md)以及[优化工具](optimization_tooling.md)。最后用 [C++ HFT 贯穿项目](hft_capstone.md)把行情、订单簿、信号、风控和网关串起来。这种顺序不是为了拖慢进度，而是因为高阶内容都建立在“对象何时存在、谁负责释放、访问是否仍然有效”之上。

如果你正准备求职，可以直接使用[六周面试学习路线](interview_roadmap.md)安排每天的阅读、编码和自测。

## C++ 与 Rust：先建立一座桥

如果你读过本书的 Rust 部分，可以暂时用下表建立映射。它只是一张导航图，不表示两种语言完全等价。

| 想表达的事 | Rust 常见写法 | C++ 常见写法 | 关键差异 |
|---|---|---|---|
| 局部拥有值 | `let x: T = value;` | `T x{value};` | 都通常随作用域结束生命周期；优化器可能改变实际存储位置 |
| 只读借用 | `&T` | `const T&` | C++ 编译器不做完整的借用检查 |
| 可变借用 | `&mut T` | `T&` | C++ 不保证同一时间只有一个可变引用 |
| 独占堆所有权 | `Box<T>` | `std::unique_ptr<T>` | 都适合表达唯一所有者 |
| 共享所有权 | `Arc<T>` | `std::shared_ptr<T>` | 计数/控制块可安全共享不等于 `T` 自动线程安全；内容仍需满足各自约束 |
| 可选值 | `Option<T>` | `std::optional<T>` | C++ 指针也可用 `nullptr` 表示“没有对象” |
| 析构清理 | `Drop` | 析构函数 / RAII | C++ 析构时机还会受所有权写法影响 |
| 移动 | 默认按 Rust 所有权规则 | 移动构造/赋值 | C++ 被移动对象通常仍存在，但状态可能改变 |

最重要的区别可以先记成一句话：

> Rust 尝试让编译器证明借用关系；C++ 给程序员更多表达自由，也把更多生命周期证明责任交给程序员。

## 贯穿案例：一条简化行情

这部分会反复使用一个很小的教学对象：

```cpp
#include <cstdint>
#include <iostream>

struct Quote {
    std::int64_t price_ticks;
    std::uint32_t quantity;
};

int main() {
    const Quote best_bid{10'025, 300};
    std::cout << "best bid: " << best_bid.price_ticks
              << " ticks, quantity: " << best_bid.quantity << '\n';
}
```

把价格写成整数 `price_ticks`，意思是“多少个最小变动单位”，而不是直接使用浮点数。例如最小变动单位为 0.01 元时，`10'025` ticks 表示 100.25 元。整数单位让示例更容易讨论溢出、布局和确定性，但真实市场的价格表示仍应以接入场所的协议为准。

代码中的单引号只是数字分隔符：`10'025` 与 `10025` 完全相同，前者更容易阅读。

## 怎样运行本部分的示例

带 `cpp` 标记的代码块都是一个完整源文件，且各自只有一个 `main`。保存为 `main.cpp` 后可使用支持 C++20 的编译器：

```bash
c++ -std=c++20 -Wall -Wextra -Wpedantic main.cpp -o demo
./demo
```

标记为 `cpp,ignore` 的代码块是局部片段、伪代码或故意展示的错误，正文会说明它为什么不能直接编译。不要把“警告为零”当成程序正确的证明；警告、静态分析、测试、Sanitizer（动态错误检查工具）和基准测试回答的是不同问题。

## 贯穿全书的成本问题

看到任何 C++ 抽象时，都可以先问五个问题：

1. 对象的数据放在哪里？
2. 谁拥有它，谁只是临时查看它？
3. 它何时被销毁，引用能否活得更久？
4. 这一步是否分配、复制、加锁或做间接跳转？
5. 这些成本是否真的发生在被测量为瓶颈的热路径？

这五个问题比背诵“某个类型快、某个类型慢”更可靠。低延迟优化不是把所有高级抽象删除，而是让关键路径上的资源和控制流更可预测，并用真实负载验证。

## 开始前的三个约定

- 本书说“通常”“常见”时，表示它可能依赖实现、编译器、平台或优化级别；不会把经验观察冒充语言保证。
- 教学算例中的纳秒、容量和地址都用于手算，不代表某台生产机器的性能承诺。
- HFT 联系用于解释工程约束，不构成交易策略、收益或特定市场规则建议。

## 小结

C++ 学习的主线不是记语法，而是理解对象、生命周期和成本。先用现代类型建立清晰所有权，再逐步学习底层机制；这样既能写出能工作的程序，也能解释它在机器上为什么这样工作。
